[project]
name = "data-processor"
version = "0.1.0"
description = "Command-line data processing tool"
requires-python = ">=3.11"

[project.scripts]
data-processor = "src.main:main"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
